// the className :"is the class name that we want to assign it to the element."
function setClass(event,idName,className) {
  let el = document.getElementById(idName);
  el.className = className;
}
// the className :"is the class name that we want to assign it to the element."
function setClass(event,className) {
  let el = document.getElementById('mypara');
  el.className = className;
}
//console.log("my code is here!!!");

//function greet(name, time) {
//  console.log('Good ' + time +" "+ name);
//}
// "change the  class name of the element with 'mypara' id to the 'warning' class"
//function setClassWarning(event) {
//let el = document.getElementById('mypara');
//el.className = 'warning';
//}
// "change the  class name of the element with 'mypara' id"
//function clearClass(event) {
//  let el = document.getElementById('mypara');
//  el.className = '';
//}
// "change the  class name of the element with 'mypara' id to the 'tip' class"
//function setClassTip(event){
//  let el = document.getElementById('mypara');
//  el.className = 'tip';
//  console.log(event);
//}
// the idName :"is connected to the element that we want to change its class."
